# Secretos y configuración local — NUNCA commitear
.env
.env.*

# Dependencias y builds
node_modules/
dist/

# Datos crudos descargados del sheet (contienen datos personales)
.data/
*.xlsx

# Fuente en claro de la sección interna cifrada (public/propuesta.html)
.private/

# JSONs generados (la Action los regenera en cada despliegue)
public/data/
